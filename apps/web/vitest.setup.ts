// jest-dom adds matchers like .toBeInTheDocument() / .toBeDisabled()
// onto Vitest's `expect`. Loaded once per test run via vitest.config's
// `setupFiles`.
import "@testing-library/jest-dom/vitest";
